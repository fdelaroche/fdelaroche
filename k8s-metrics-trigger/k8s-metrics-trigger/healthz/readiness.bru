meta {
  name: readiness
  type: http
  seq: 2
}

get {
  url: {{host}}/healthz/readiness
  body: none
  auth: inherit
}

tests {
  test("status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("content should be OK", function() {
    expect(res.getBody()).to.equal("OK");
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
