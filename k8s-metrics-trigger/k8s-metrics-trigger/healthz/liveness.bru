meta {
  name: liveness
  type: http
  seq: 1
}

get {
  url: {{host}}/healthz/liveness
  body: none
  auth: inherit
}

tests {
  test("status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("content should be OK", function() {
    expect(res.getBody()).to.equal("OK");
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
